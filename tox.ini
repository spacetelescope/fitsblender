[tox] 
isolated_build = true
envlist = py39
skip_missing_interpreters = true

[pytest]
testpaths =
    fitsblender/tests

[testenv]
changedir = {toxinidir}

setenv =
    PYTHONPATH = {toxinidir}

deps =
    pytest

commands =
    pytest -s fitsblender/tests
